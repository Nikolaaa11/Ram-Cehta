# Types

Este directorio contiene tipos TypeScript compartidos.

## `api.ts` (auto-generado)

**NO editar a mano.** Se genera desde `../backend/openapi.json` con:

```bash
npm run gen:types
```

El CI falla si `types/api.ts` está desincronizado con el OpenAPI del backend (Disciplina 5).
