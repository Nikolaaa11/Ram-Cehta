# Health Check — Ram-Cehta (FIP CEHTA ESG)

**Fecha:** 2026-07-14
**Backend:** https://cehta-backend.fly.dev
**Ejecución:** tarea programada diaria (automática)

## Status global: 🟢 HEALTHY

Plataforma operativa. `/health` responde 200, todas las rutas protegidas devuelven 401 (existen, auth-gated), y el total de rutas del OpenAPI = **432 paths**, idéntico al baseline conocido post-barrido. Sin errores 5xx. Sin caídas.

## Endpoints

| Código | Endpoint | Resultado |
|--------|----------|-----------|
| 200 | `/health` | ✅ OK |
| 401 | `/api/v1/admin/feedback/summary` | ✅ OK (existe, auth-gated) |
| 401 | `/api/v1/admin/adoption/map` | ✅ OK |
| 401 | `/api/v1/training/modules` | ✅ OK |
| 401 | `/api/v1/admin/corfo/catalogos` | ✅ OK |
| 401 | `/api/v1/calendar/obligations` | ✅ OK |
| 401 | `/api/v1/vouchers` | ✅ OK |
| 404 | `/api/v1/transferencias` | ⚠️ Path del checklist incorrecto (ver nota) |
| 404 | `/api/v1/me` | ⚠️ Path del checklist incorrecto → real: `/api/v1/auth/me` (401) |
| 404 | `/api/v1/dashboard/institucional` | ⚠️ Path del checklist incorrecto (ver nota) |

**Resumen:** 7/10 OK directo · 3/10 con 404 por **rutas mal escritas en el checklist**, NO por fallas de la plataforma.

### Nota sobre los tres 404 (verificado, no son incidentes)
Investigué cada uno contra el OpenAPI en vivo y con probes de rutas alternativas:

- **`/api/v1/me`** → no existe como ruta suelta. La real es **`/api/v1/auth/me`** → devuelve **401** (existe). (Sí existen sub-rutas `/api/v1/me/2fa/*`.)
- **`/api/v1/dashboard/institucional`** → no existe ese sub-path. El dashboard expone `cashflow`, `compliance`, `impact`, `kpis`, `lps`, `portfolio`, `fund/metrics`, `vouchers-kpis`. `/api/v1/dashboard` (base) devuelve **401** y `/api/v1/dashboard/fund/metrics` devuelve **401**.
- **`/api/v1/transferencias`** → no existe ninguna ruta `transfer*` en la API. Coincide con el diseño del fondo: no hay nómina/transferencias como recurso propio; las transferencias se ordenan desde **vouchers aprobados** y **ordenes-compra** (rutas `/api/v1/vouchers` y `/api/v1/ordenes-compra/*`, ambas 200/401).

Conclusión: los tres son **falsos negativos del checklist**, no degradación. La plataforma está intacta (432/432 paths).

## Cambios del día anterior (git log --since="1 day ago")
Sin commits en las últimas 24 h. Últimos commits en el repo (contexto, previos):
- `a1066e0` MEGAPROMPT F1a: registrar test E2E revocación verde (5/5)
- `04b3ae2` MEGAPROMPT F1a: quitar gate 2FA del DELETE /admin/users (revocación reversible)
- `e0e97c9` MEGAPROMPT F1a: revocación de acceso REAL + F2.1 usuaria Caterin
- `379b086` R152WWWWWW: formato Panimávida = diseño estándar de OC para todas las empresas
- `101bad1` R152VVVVVV: área de Gastos rápidos (Marketing/Desarrollo) + sidebar

## Acciones sugeridas
- Ninguna urgente — plataforma healthy.
- **Housekeeping (opcional):** corregir las 3 rutas del checklist de esta tarea programada para evitar 404 recurrentes en futuros reportes:
  - `/api/v1/me` → `/api/v1/auth/me`
  - `/api/v1/dashboard/institucional` → `/api/v1/dashboard/fund/metrics` (o el sub-dashboard que se quiera vigilar)
  - `/api/v1/transferencias` → quitar (no existe) o reemplazar por `/api/v1/ordenes-compra` o `/api/v1/vouchers`
